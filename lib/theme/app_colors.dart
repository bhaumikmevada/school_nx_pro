import 'package:flutter/material.dart';

class AppColors {
  static const blue = Color(0xFF0086d5);
  static const bgColor = Color(0xFFFEFBFF);
  static const blackColor = Color(0xFF000000);
  static const whiteColor = Color(0xFFFFFFFF);
  static const textHintColor = Color(0xFF727A8D);
  static const colorDADADA = Color(0xFFDADADA);
  static const colorcfcfcf = Color(0xFFCFCFCF);

}

// default padding
// const double defaultPadding = 16;

// default elevation
// const double defaultElevation = 8;

// default iconButton splash radius
// const double defaultSplashRadius = 20;

// default circularRadius
// const double defaultCircularRadius = 15;

// default Radius Card
// const double defaultCircularRadiusCard = 10;
